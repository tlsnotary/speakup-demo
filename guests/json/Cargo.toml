[package]
name = "json-guest"
version = "0.0.0"
publish = false
edition = "2021"

[lib]
# cdylib for the wasm artifact; rlib so `cargo test` can link a test harness.
crate-type = ["cdylib", "rlib"]

[dependencies]
mpz-vm-sys = { workspace = true }
transcript-core = { path = "../transcript-core" }
